name: HelloWorld

 

on: workflow_dispatch

 

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo "hello name"
      - name: goodbye
        run: echo "goodbye"
